# Task 10 — Log

## [2026-08-23] ox-alpha-s10
- **What I learned:** The Tauri app lives at `scholiast_tauri/` (task brief's `src-tauri/...` paths are relative to it). task-09 shipped the recorder: `useVoiceRecorder.stop()` returns `{path, reason}` but `MicButton` swallows the path internally — it exposes only `onStateChange/onError`. Commands here return `Result<Reply<T>, ScholiastError>` (Ok enveloped `{ok:true,data}` by Reply; Err rejects with `{ok:false,error:{kind,message}}` from ScholiastError's custom Serialize). reqwest is 0.13 with features `[charset, rustls]` only — multipart NOT enabled. No keyring dep yet.
- **Decisions made:** (1) Core's `ScholiastError` taxonomy (db/notFound/invalidInput/io/internal) has no `no_provider`/`gemini_required` kinds and `crates/core/error.rs` is not mine — so cloud.rs defines its own `SttError` serializing the identical `{ok:false,error:{kind,message}}` shape with kinds `noProvider`... actually exact strings per spec: `no_provider`, `gemini_required` (+ `http`, `network`, `io`, `parse`). (2) Transcriber trait uses native async-fn-in-trait (rustc 1.88) instead of adding an async-trait dep; no dyn dispatch needed (command constructs the client directly). (3) Prompt seeding deferred to settings task; public consts exported instead (chosen simpler option). Model prefs ARE read in command context via tauri_plugin_store (read-only — no Rust-side pref writes, so no store-change-event hazard per AGENTS.md).
- **Open questions:** none yet.
- **Progress:** Context fully mapped. Writing cloud.rs next.

## [2026-08-23] ox-alpha-s10 — Rust side done
- **What I learned:** reqwest 0.13 here runs with `default-features=false` and no `json` feature — `.json()` on RequestBuilder doesn't exist; transcript/client.rs idiom is manual `Content-Type: application/json` + `serde_json::to_string` body + `.text()` parse, so cloud clients match it (avoids widening the feature set). tauri-plugin-store v2's Rust-side trait is `StoreExt` (`app.store("settings.json")`), not `ManagerExt`. wiremock gotcha: my first Gemini test passed the bare `server.uri()` as the endpoint base and got a 404 because the prod base const already contains `/v1beta/models` — test bases must include the version path.
- **Decisions made:** (1) `stt_edit_text` resolves its instruction as **override → pref `prompt.edit_comment` → plan-default const** in the command itself (matches task spec wording "system instruction = pref … default EXACT text"); this also gives `PREF_EDIT_PROMPT` a real non-test use. (2) Gemini's trait-level `transcribe` implements the plan §6.5.2 *prompted add-comment* branch (audio + Add-comment prompt, no note part) via a shared `generate()`; `edit_text` adds the original-note text part. (3) Targeted `#[allow(dead_code)]` on `Transcriber` trait + `Caps::contains` with a rationale comment — same idiom recording.rs uses for deferred-integration API (task-11 local whisper + Settings "Test connection" hang off them). (4) Prompt/model prefs are read-only from Rust (`settings.json` via StoreExt, silent fallback to defaults); **seeding defaults stays with the Settings task** — chosen simpler option, avoids Rust-side store writes that wouldn't emit the JS change event (AGENTS.md hazard).
- **Open questions:** none.
- **Progress:** GATES: `cargo clippy --workspace --all-targets -- -D warnings` CLEAN · `cargo test --workspace` 28/28 green (8 new: groq happy multipart+params, groq 401 provider-agnostic, gemini happy candidates-parse + body asserts incl. base64 round-trip, gemini missing-key→`gemini_required`, groq missing-key→`no_provider`, missing-wav→invalidInput, caps flags, error-envelope kinds) · `cargo check -p scholiast --features local-stt` still compiles. Frontend VoiceEditSheet next.

## [2026-08-23] ox-alpha-s10 — FINAL
- **What I learned:** MicButton's internal recorder hides the stop path from callers; the one backwards-compatible prop it needed was `onStopped?: (result: StopResult) => void` (toggle now routes through `stopAndReport`, which awaits `recorder.stop()` and then reports — existing call sites unaffected). Frontend error handling must cover TWO shapes: tauri rejects commands with the RAW serialized error `{ok:false,error:{kind,message}}` (invokeCommand does not catch rejections), so VoiceEditSheet's `toErrorInfo` handles both that object and `IpcCommandError`. The Gemini happy-path test initially 404'd because the endpoint-base seam excludes `/v1beta/models` (prod const includes it).
- **Decisions made:** (1) Prompt-edit instruction resolution lives in the command: `prompt_override` → pref `prompt.edit_comment` → `EDIT_PROMPT_DEFAULT` (exact plan §6.5.6 text). (2) Seeding defaults into the store is DEFERRED to task-19 (Settings); cloud.rs exports the consts publicly (`ADD_PROMPT_DEFAULT`, `EDIT_PROMPT_DEFAULT`, `DEFAULT_GROQ_MODEL`, `DEFAULT_GEMINI_MODEL`, pref/key name consts) — the chosen "simpler" option; Rust never writes prefs here. (3) `SttError` is cloud.rs-local with kinds `no_provider | gemini_required | http | network | io | parse | invalidInput`, same wire envelope as ScholiastError. (4) Transcriber trait via native async-fn-in-trait, `Caps{VERBATIM=1|PROMPTED=2}` bitset; targeted `#[allow(dead_code)]` on trait + `Caps::contains` (task-11/Settings pings will consume). (5) KeyProvider seam: trait `fn key(&entry)`; prod impl = keyring crate, service `scholiast`, entries `groq.api_key`/`gemini.api_key`; tests inject `StaticKeys`. Keys never logged; HTTP errors render provider-agnostically ("Speech failed: HTTP 401"). (6) Gemini trait-level `transcribe` = prompted add-comment branch (audio + Add-comment prompt only); `edit_text` appends the original-note text part.
- **VoiceEditSheet usage for task-07 integration** (exported ready, NOT wired into CommentEditorSheet per instructions):
  ```tsx
  import VoiceEditSheet from "../voice/VoiceEditSheet";
  <VoiceEditSheet
    original={commentBody}                       // current note text
    initialPrompt={promptFromStoreEditComment}   // read pref `prompt.edit_comment` via JS store plugin
    micDisabledReason={hasAnyProvider ? null : "Set up speech in Settings"}
    onAccept={(edited) => saveWithStamp(edited)} // caller stamps `<!--edited:N-->` (task-07 owns stamping)
    onDiscard={() => close()}
  />
  ```
  Readiness detection: no command exists yet to query keyring presence — Settings task should add e.g. `stt_provider_status`; until then callers pass a static reason or omit the prop. Add-comment verbatim flow (§6.5.2): call `stt_transcribe({ wavPath, language })` directly; no sheet involved.
- **Open questions:** (a) `stt_provider_status`-style readiness command needed before the sheet can self-disable without caller input. (b) keyring v3 on Linux needs a running secret service (gnome-keyring/kwallet) — untested on headless CI; tests avoid the OS keyring by design. (c) Groq/Gemini 429 test path folded into the retry test at the UI layer (wiremock covers 401; 429 shares the Http(u16) arm).
- **Progress:** ALL GATES PASS · Gate A `cargo clippy --workspace --all-targets -- -D warnings` CLEAN · Gate B `cargo test --workspace` all suites ok (8 new cloud tests) · Gate C `pnpm lint` clean · Gate D `pnpm typecheck` clean · Gate E `pnpm vitest run` 9 files / 74 tests passed (4 new: accept→onAccept(edited), discard, retry-after-failure incl. empty-prompt→null normalization, disabled-reason passthrough) · `cargo check -p scholiast --features local-stt` still compiles. Files touched: src-tauri/src/stt/cloud.rs (new), stt/mod.rs (+1 line), lib.rs (+2 registrations), Cargo.toml (reqwest multipart feature + keyring dep), src/components/MicButton.tsx (optional onStopped), src/voice/VoiceEditSheet.tsx + .test.tsx (new), task.md, LOG.md. Task → DONE.
