# LOG — task-12 Transcript Fetch & Parse Core

## [2026-08-23 00:15] task-12 agent (transcript-core)
- **What I learned:**
  - The desktop extension has **no json3 parser**: `src/utils/video/video-transcript.ts` parses caption **XML** only (`parseCuesXml`, lines 203–230). The authoritative json3 semantics are the Kotlin port `android/.../domain/transcript/CueParser.kt:51–95`: events with `tStartMs` start a cue (`end = tStartMs + dDurationMs`); events without `tStartMs` (i.e. `aAppend`) accumulate their `segs[].utf8` into the previous cue's text (plain concatenation — segs carry their own whitespace; `tOffsetMs` is word timing and is ignored for text assembly), extend the end when a positive `dDurationMs` is present, and never create cues. Cue text is trimmed at flush; blank-text cues dropped.
  - Chunker thresholds live at `video-transcript.ts:253–255`: `TRANSCRIPT_GROUP_GAP_SECONDS = 20` (:254), `TRANSCRIPT_MAX_GROUP_SECONDS = 30` (:255), `SENT_END = /[.!?。！？]["')\]’”]?\s*$/` (:253). `semanticChunk` itself is :439–454. Desktop calls it directly on its cue list (:490); `splitOnInternalSentences` (:399–422) exists but is unused there — the Kotlin chunker (`TranscriptChunker.kt:86–87`) wires it as `chunk() = semanticChunk(splitOnInternalSentences(cues))`.
  - `pickTrack` full precedence (`video-transcript.ts:179–189`, mirrored `TranscriptClient.kt:243–251`): session pref exact `languageCode` match → English-prefix (`en…`) non-ASR → first English (ASR acceptable) → any-language non-ASR → first track. Empty list → null.
  - IOS→WEB fallback trigger (`video-transcript.ts:158–165`): the client flips to WEB whenever the IOS attempt yields **zero usable tracks** — network failure, non-ok HTTP, unparseable JSON, missing `captions.playerCaptionsTracklistRenderer.captionTracks`, or an empty list after dropping baseUrl-less tracks. Note: the Kotlin client bails early on non-2xx instead of trying WEB (`TranscriptClient.kt:124`); I follow the TS semantics (fallback on every failure shape) since the task pins this to TS.
  - Innertube constants (`video-transcript.ts:144–146`): `POST https://www.youtube.com/youtubei/v1/player?prettyPrint=false`; IOS context `{clientName:"IOS", clientVersion:"20.10.3"}`; WEB `{clientName:"WEB", clientVersion:"2.20240101.00.00"}`; body `{"context":{...},"videoId":...}`.
  - No json3 fixtures exist anywhere in the repo (grepped extension tests, android tests). Fixtures will be crafted from the real-world json3 sample documented in `CueParser.kt:39–46`.
  - `src-tauri/src/lib.rs` declares no `transcript` module yet (same situation as the sibling `stt` module) — integration deferred per file-ownership rules.
- **Decisions made:**
  - `Cue { start: f64, end: f64, text: String }` in **seconds** (task spec + extension `TranscriptCue` units); json3 ms values divided by 1000.
  - `Paragraph { index, text, start, end, cue_range:[usize;2] }`, serde camelCase for IPC, mirroring `TranscriptChunker.buildParagraph` (Kotlin :89–96).
  - Client pipeline calls `chunk()` (= split + semantic_chunk) to match the Android sibling client's behavior; grouping thresholds untouched. Logged as deliberate deviation from plan §6.6's bare "semanticChunk" wording.
  - `regex` dep added to `crates/core` (needed for XML fallback + entity decode); `reqwest` (json, rustls-tls) added to `src-tauri`, `wiremock` as dev-dep. Manifest edits are additive-only.
  - `fetch_transcript` declared `#[tauri::command] #[allow(dead_code)]` in `client.rs`, NOT registered; disk cache behind explicit `cache_dir: PathBuf` param (state wiring deferred).
- **Open questions:** none blocking.
- **Progress:** sources analyzed, status flipped to IN PROGRESS, writing `crates/core/src/cue.rs` next.

## [2026-08-23 00:40] task-12 agent (transcript-core)
- **What I learned:** Rust regex has no lookahead, so `splitOnInternalSentences`'s lookahead was re-implemented as a manual char scan (punct → optional closing quote → `\s+` → uppercase/opening-quote). Verified the TS regex requires `\s+`, so space-less CJK boundaries do NOT split — my port matches TS even though the Kotlin doc-comment implies otherwise. Gap flush uses strict `>` (20.0s gap does NOT break); max-span uses `>=` 30s from pending[0].
- **Decisions made:** `Paragraph.cueRange` = slot indexes into the cue array returned by the pipeline (Cue carries no `index` field per task spec `{start,end,text}`); split cues are what the client returns so anchors stay aligned. Golden chunker fixture committed (`crates/core/tests/fixtures/chunker/`) exercising all three flush signals; caption fixtures under `fixtures/captions/`.
- **Progress:** `crates/core/src/cue.rs` complete; `cargo test -p scholiast-core` → 28 passed / 0 failed; clippy `-D warnings` clean. Starting `src-tauri/src/transcript/{mod,client}.rs` + wiremock tests.

## [2026-08-23 00:55] task-12 agent (transcript-core)
- **What I learned:** A sibling task (local-stt) had already added `reqwest 0.13` (optional) + `sha2` + `whisper-rs` to `src-tauri/Cargo.toml` behind the `local-stt` feature — shared-manifest race. Also `wiremock::Request.body` is a plain `Vec<u8>` (not Option) and `.url` is a `Url` (use `.as_str()`); wiremock 0.6 matchers used: `body_string_contains`, `query_param`. House pattern for undeclared modules: sibling `stt/*` keeps tests as inline `#[cfg(test)] mod tests` inside the module file (dormant until integration wires `mod`), so I moved my wiremock tests inline and deleted `src-tauri/tests/`.
- **Decisions made:**
  - Manifest: reqwest made non-optional with the sibling's exact features (`charset`,`rustls`) — no `json` feature needed (bodies parsed via `serde_json::from_str`); `dep:reqwest` removed from the sibling's `local-stt` list (its other deps untouched). Additive-only otherwise: `wiremock = "0.6"`, `tempfile = "3"` to `[dev-dependencies]`; `regex = "1"` to crates/core.
  - Module verification without touching forbidden `lib.rs`: full workspace copy at /tmp/opencode/task12_verify with `pub mod transcript;` added THERE only; clippy + `cargo test -p scholiast` run against it sharing the real target dir; copy deleted after. Real `lib.rs`/`state.rs` confirmed untouched (0 occurrences of "transcript").
  - Cache flow mirrors TS `loadTranscript`: track discovery precedes cache lookup, so a cached fetch still POSTs `/player` once per client (asserted: exactly one captions download across two clients).
- **Progress:** DONE.
  - `crates/core/src/cue.rs`: `Cue{start,end,text}` (seconds), `Paragraph{index,text,start,end,cueRange}`; `parse_json3` (tStartMs/aAppend semantics from Kotlin CueParser.kt:51–95), `parse_xml` srv3+timedtext port of TS :203–230 incl. collapse-then-decode order; `semantic_chunk` verbatim TS :439–454; `split_on_internal_sentences` (manual scan replacing the lookahead regex); `chunk()` pipeline.
  - `src-tauri/src/transcript/{mod,client}.rs`: innertube IOS→WEB (fallback on any zero-tracks failure shape, TS :158–165), `extract_caption_tracks`, `pick_track` full ladder (TS :179–189), json3 fetch (`&fmt=json3` appended, Accept-Language), typed `TranscriptResult{lang,paragraphs,cues}`, `TranscriptError{NoCaptions,Network,Http,Parse}` → ScholiastError mapping (NotFound/"no captions"), 32-slot LRU + disk cache `<cache_dir>/transcripts/<videoId>_<lang>.json`; unregistered `#[tauri::command] fetch_transcript(video_id, lang_pref, cache_dir)` with `#[allow(dead_code)]`.
  - Gates: real workspace `cargo clippy -p scholiast-core -p scholiast --all-targets -- -D warnings` CLEAN; `cargo test -p scholiast-core` 46 passed / 0 failed (8 new). Wired-copy proof: clippy clean + `cargo test -p scholiast` 9/9 incl. 4 wiremock tests (IOS→WEB fallback w/ request-order asserts, NoCaptions, pickTrack matrix, disk-cache).
- **Open questions (integration task):** declare `pub mod transcript;` in src-tauri lib.rs and register `fetch_transcript` in the invoke handler; move `cache_dir` into AppState then drop the explicit param; consider checking mem/disk cache before track discovery when `lang_pref` is provided (saves 2 POSTs on cache hits).
