# 09-voice-recorder — Agent log

Log entries appended while working on this task (see README.md, "Agent logging protocol").

## [2026-08-19 18:15] task-09 agent
- **What I learned:** `android/` has NO Gradle build yet (Task 01 NOT STARTED — only `AGENTS.md` + empty `src/`). No `build.gradle.kts`, no `libs.versions.toml`, no dependencies. Tests cannot run locally yet. Task 05 (PlayerBridge) is also NOT STARTED — no `PlayerBridge` interface exists to call, so I expose `onPauseRequested`/`onResumeRequested` callbacks on the ViewModel as the task allows. FUTO `AudioRecognizer.kt` reviewed: 16 kHz mono PCM-16 `VOICE_RECOGNITION`, `AudioRecord` buffer 16000*2*5, 1600-sample blocking reads, float accumulation into a growing `FloatBuffer` (+30 s), magnitude `1 - 0.1^(24*rms)`, `AudioFocusRequest(GAIN_TRANSIENT_EXCLUSIVE)`, stop = `recorderJob.cancel()` + `recorder.stop()`.
- **Decisions made:** Port only the recording loop + focus handling; skip VAD / Bluetooth SCO / mic-blocked / model-runner (those are Task 10/11). Pure, JVM-testable core (`AudioCapture` + `RecordingDriver`) owns buffer growth + magnitude + the state machine; `AndroidVoiceRecorder` is a thin `AudioRecord` shell. `stop(): FloatArray` is synchronous — snapshot happens after the read loop is cancelled and the capture is closed, so no write race. `Processing` is set by the ViewModel after stop (while a transcriber runs) then returns to `Idle`; `Stopped(samples)` is the terminal state carrying the samples. WAV encoder written pure-Kotlin (`WavWriter`) so it is JVM-testable; caller passes `cacheDir/voice`. Permission flow: ViewModel checks permission; if missing emits a `RequestPermission` event; the owning screen shows a rationale dialog + `ActivityResultLauncher`, then calls `onPermissionResult(granted)`; denied → `Error` state + settings link. Rotation survival via `AndroidViewModel` + `viewModelScope`.
- **Open questions:** PlayerBridge (Task 05) absent — auto-pause/resume will wire to `onPauseRequested`/`onResumeRequested` lambdas in the integration pass. Task 10 decides the exact WAV consume path; `encodeWav(samples, cacheDir)` is provided for Groq upload, `FloatArray` direct for local STT.
- **Progress:** Task marked IN PROGRESS. Reading plan §2/§5.5.1/§6.2/§9 M2 and FUTO source complete. Writing `ui/voice/` files next.

## [2026-08-19 18:55] task-09 agent
- **What I learned:** `android/` has no build (Task 01 NOT STARTED) and no Android SDK/android.jar on this machine, so I verified by compiling all 4 source files + the test file with the Gradle-distributed kotlin-compiler-embeddable (2.0.20, run on JDK 17 — JDK 25 breaks it) against hand-written stubs of the android/androidx/compose APIs (in `/tmp/opencode/ktcheck/stubs`), then ran the tests with a stub JUnit runner. The stub compile caught 3 genuine bugs: `dragAmount.y` (it's a Float, not Offset), `Long.roundToInt()` doesn't exist (`toInt()`), and `assertEquals(RecorderState.Recording, …)` needs `is` since Recording is a data class. Also learned Kotlin rejects `3.dp.toPx()` unless toPx is resolvable as `Density.toPx(value: Dp)` with the Dp bound to the parameter — the reliable form is `toPx(3.dp)` (used in MicButton).
- **Decisions made:** Test file placed in the standard test source set `app/src/test/java/...` (NOT the main path listed in task.md) so it never ships in the APK — Task 01's Gradle setup picks it up automatically via `:app:testDebugUnitTest`. `AudioRecord` buffer = `max(getMinBufferSize, 16000*2*5)` (FUTO's 5 s size, floored to the device minimum — more robust than FUTO's hardcode). RMS computed over the actual read count (`nRead`) rather than the fixed 1600. Auto-stop flag (`lastStopWasAuto`) is separate from the interface so the ViewModel can toast; it is reset in `start()`, never in `stopInternal()` (a bug I caught while writing). `AndroidVoiceRecorder` has a private primary constructor (takes the internal `RecordingDriver`) + internal `constructor(context)`; ViewModel and tests construct via those.
- **Open questions:** PlayerBridge (Task 05) still absent → ViewModel exposes `onPauseRequested`/`onResumeRequested` lambdas for the integration pass to wire to `bridge::pause`/`bridge::play`. Task 10 must decide Groq's exact upload path; `WavWriter.encodeWav(samples, cacheDir)` + `VoiceRecorderViewModel.encodeWav(samples)` are ready for it. `AudioCapture.close()` is currently only exercised by tests (the stop path relies on the driver's synchronized finish guard) — kept as a sealing API.
- **Progress:** All files written; compile-verified (0 errors) and all 12 JVM tests pass: buffer growth past 30 s, max-length guard at exactly 2 min (feed refused, position == 1_920_000), magnitude in range + ordering, close/reset semantics, full state machine (Idle→Recording→Stopped→Processing→Idle, cancel discards, fail→Error, finish-from-idle), stop-returns-exact-samples, WAV header (RIFF/fmt/data, 16 kHz mono 16-bit, byte-exact sample values), encodeWav file naming/location, quantize clamping. Full pass output recorded in compile.log/run under /tmp/opencode/ktcheck (harness kept for the next agent; not part of the repo).

## [2026-08-19 19:10] task-09 agent — final summary
- **Delivered** (all under `android/app/src/main/java/com/scholiast/android/ui/voice/`, test under `android/app/src/test/java/com/scholiast/android/ui/voice/`):
  - `VoiceRecorder.kt` — `RecorderState` sealed interface, `VoiceRecorder` interface, pure JVM-testable `AudioCapture` (growable float buffer 30 s→+30 s steps, 2 min cap, magnitude `1-0.1^(24*rms)`), pure `RecordingDriver` state machine, `AndroidVoiceRecorder` (AudioRecord 16 kHz mono PCM-16 VOICE_RECOGNITION, FUTO read loop port incl. catch-up drain, `AUDIOFOCUS_GAIN_TRANSIENT_EXCLUSIVE`, max-length auto-stop, `fail`), `WavWriter` (16 kHz mono 16-bit PCM WAV → `cacheDir/voice/<ts>.wav`).
  - `VoiceRecorderViewModel.kt` — AndroidViewModel owning the recorder (rotation-safe, `viewModelScope`), tap-to-toggle, permission check → `VoiceEvent.RequestPermission` → `onPermissionResult`, denied → Error + settings-link event, `onPauseRequested`/`onResumeRequested`, `onSamplesReady` + `markProcessing`/`onTranscriptionDone`, `encodeWav`.
  - `MicButton.kt` — tap-to-toggle UI: pulsing red ring (respects animator-duration-scale=0) + elapsed M:SS, swipe-down cancel (48 px threshold, cumulative), states, settings tap on Error, canvas-drawn mic glyph (no icon dependency), TalkBack descriptions, 72 dp target.
  - `VoiceRecorder.Interface.md` — full contract doc for Tasks 07/10/11 (states, transitions, audio format, WAV, focus, permission).
  - `VoiceRecorderTest.kt` — 12 JVM tests, all passing (see previous entry).
- **Build gap:** no Gradle build exists (Task 01 NOT STARTED) — `./gradlew :app:assembleDebug` / `:app:testDebugUnitTest` cannot run yet. Verified instead via kotlin-compiler against API stubs (see entry above). First real build may need: `RECORD_AUDIO` already in Task 01's manifest plan; `androidx.core:core-ktx` (ContextCompat), lifecycle-viewmodel-ktx, compose BOM in the version catalog.
- **Contract in brief:** `VoiceRecorder { val state: StateFlow<RecorderState>; fun start(scope: CoroutineScope); fun stop(): FloatArray; fun cancel() }` with `RecorderState = Idle | Recording(elapsedMs, magnitude) | Processing | Stopped(samples) | Error(msg)`; `encodeWav(samples, cacheDir): File` for Groq; FloatArray direct for local STT.
- Task status → DONE.


## [2026-08-19 19:45] task-09 agent — RESUME/VERIFY (real Gradle build)
- **What I learned:** Task 01's Gradle build now exists with **dev/prod product flavors** (dimension `environment`), so `:app:testDebugUnitTest` is ambiguous — the real task names are `:app:testDevDebugUnitTest` and `:app:testProdDebugUnitTest` (and `:app:assembleDebug` builds both flavors). JDK 25 is the system default and breaks the Kotlin compiler; use `JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64` (JDK 21 also present). **Task 12's `TranscriptClientTest.kt` fails the whole test source set to compile**: it imports `okhttp3.mockwebserver.*`, which is NOT in `gradle/libs.versions.toml` (only `okhttp = 4.12.0`) and not in `app/build.gradle.kts` (`testImplementation` has only junit). 26 errors, all in that file; not mine to fix (logged per rules).
- **Decisions made:** One-line fix in my own test file: `VoiceRecorderTest.kt:215` `file.parentFile.parentFile` → `file.parentFile?.parentFile` (compiler warning, nullable receiver). No other changes. Task 12's file was never edited — I verified my tests by temporarily moving it out of the source set (restored byte-identical; git confirms no diff).
- **Open questions:** Task 01/12 must add `okhttp3:mockwebserver` (4.12.0) to the version catalog + `testImplementation` in `app/build.gradle.kts`, or `./gradlew :app:test*UnitTest` fails at compile for every task until then. My voice tests are unaffected once that lands.
- **Progress:** `./gradlew :app:assembleDebug` → **BUILD SUCCESSFUL** (my files compile in the real build, no errors/warnings). `:app:testDevDebugUnitTest` + `:app:testProdDebugUnitTest` → **12/12 tests pass, 0 failures, 0 errors, 0 skipped** in both flavors (XML: `build/test-results/test{Dev,Prod}DebugUnitTest/TEST-com.scholiast.android.ui.voice.VoiceRecorderTest.xml`). Test report: buffer growth past 30 s, 2-min guard, magnitude, close/reset, full state machine, cancel, fail, finish-from-idle, exact samples, WAV header/encodeWav/quantize. task.md stays DONE.
- **Integration notes for Tasks 07/10/11:** contract unchanged (`VoiceRecorder.Interface.md`). Real build confirms `AndroidVoiceRecorder`, `WavWriter`, `VoiceRecorderViewModel`, `MicButton` all compile against Task 01's catalog (compose BOM, lifecycle, core-ktx present — no missing deps). `onPauseRequested`/`onResumeRequested` still the video-pause hook until Task 05's PlayerBridge lands. Task 10: `WavWriter.encodeWav(samples, cacheDir)` → `cacheDir/voice/voice-<ts>.wav`; the dev-flavor unit-test task name pattern (`testDevDebugUnitTest`) is what Task 10/11 should use for their own JVM tests.
