# 02-shared-data-model — Agent log

Log entries appended while working on this task (see README.md, "Agent logging protocol").

## [2026-08-19 21:10] task-02 agent (fresh session; previous session produced nothing)
- **What I learned:**
  - Task 01's build already wires everything this task needs: KSP plugin, `room-compiler` via KSP, `room-runtime`+`room-ktx`, kotlinx-serialization-json 1.9.0, and `ksp { arg("room.schemaLocation", "$projectDir/schemas") }`. No version-catalog or build-file edits needed for Room/serialization.
  - `data/normalize/Normalize.kt` (Task 03, already landed) provides `normalizeUrl` + `urlHash` (SHA-256 first 16 bytes, hex) — the repo key/hash functions this task's repository consumes.
  - TS shapes confirmed from source: `PageSource` = `{url, title, markdown, capturedAt}` (`src/utils/page-source.ts`); `tag_index` stores tags WITHOUT the `#` prefix, sorted, union-only (`comment-overlays.ts` `KNOWN_TAG_RE` captures `m[2]`); notes format `text<!--timestamp:N--><!--edited:M-->` (`video-notes.ts`).
  - TS `JSON.stringify` emits integral doubles as integers (`1` not `1.0`); kotlinx emits `1.0` by default → needed a JS-compatible Double serializer (see Decisions).
  - TS key order in emitted JSON follows object-literal construction order; `frame` omits `dataUrl` on write (stripped in `video-storage.ts` upsert); `notes: []` is always written for comment-less items.
- **Decisions made:**
  - **`ScholiastJson`** (single shared `Json` instance): `encodeDefaults = true` + `explicitNulls = false` + `ignoreUnknownKeys = true`. Together these reproduce TS semantics: `notes: []` always emitted, null/undefined fields omitted, unknown fields tolerated on read.
  - **`JsDoubleSerializer`** applied to every `Double` field in the model: integral doubles encode as integers (`x2: 1.0` → `"1"`), byte-identical to `JSON.stringify`. Guards: finite, `value == floor(value)`, `abs < 1e21`. Fractional values encode normally. (Deviation from plan §4.2? No — plan doesn't specify; this is required for byte-exactness.)
  - **`FrameImage` field order is `dataUrl, driveId, w, h`** (TS declaration order), not the plan sketch's `w, h, driveId` — so emitted JSON matches TS byte-for-byte. `dataUrl` is kept as a nullable field (never written — `explicitNulls` omits it; mirrors desktop's strip-on-write) so in-memory desktop items parse losslessly. DEVIATION from plan §4.2 literal sketch, logged per protocol.
  - **Opaque page entities preserve unknown fields**: `PageHighlight`, `PageStroke`, `PageDiagram` carry `extras: JsonObject` via a custom `ExtrasPreservingSerializer` (TS `[k: string]: unknown` spread semantics). Without this, the app's sync merge round-trip would silently drop desktop highlight fields (xpath, groupId, anchor, …). Task 17's merge port gets `{...newer, notes}` semantics for free via `copy(notes=…)` + extras riding along.
  - **Class names**: markup classes are `Stroke`/`Line`/`TextLabel`/`Rect`/`Arrow` (per task.md + plan §4.2; TS names `VideoStroke`… bridged in KDoc). PageRecord mirror is class **`VideoPage`** (task.md's name) with `typealias PageRecord = VideoPage` for the sync task. PageRecord's `highlights`/`drawings` entities are `PageHighlight`/`PageStroke` (TS `Highlight`/`Stroke` — renamed to avoid clashing with the markup `Stroke`).
  - **Room schema v1**: 3 tables `video_pages`, `ocr_texts`, `sync_meta`; JSON columns are raw TEXT (`itemsJson`, `snapJson`) with `JsonTypeConverters` that also let DAO query results come back typed (`LoadedVideoPage` POJO via `@Transaction`). `SyncMetaEntity` is generic key-value (`key` PK, `value`, `updatedAt`) — the `tag_index` lives there (JSON string array, no `#` prefix, sorted, union-only, mirroring the desktop key).
  - **Repository mutation semantics mirror `video-storage.ts`**: `addItem`/`updateItem` = upsert-by-id (stamp `updatedAt=now`, sort by `videoTime`, desktop's `upsertVideoItem` behavior); `deleteItem` removes the page row when the last item goes (desktop `removePage`); `addItem` creates the page row if absent, `updateItem` no-ops (desktop `updateVideoItemNotes` early-returns). Page-row `updatedAt` = last item mutation, not page open.
  - Page mutations are read-modify-write WITHOUT `db.withTransaction` (repo takes constructor-injected DAOs per spec). Single-process; Room serializes SQL writes; the interleave window is theoretical. Task 17/18 (sync worker) should harden with `withTransaction` if needed — noted for them.
- **Open questions:** none blocking. (Frame JPEG deletion on `deleteItem` is Task 14's `data/frames/` concern, not this task's.)
- **Progress:** task claimed (already IN PROGRESS from prior session); environment recon + TS shape audit done. Writing DTOs next.

## [2026-08-19 22:05] task-02 agent — implementation + verification
- **What I learned:**
  - KSP/Room/serialization were ALREADY wired by Task 01 (`ksp(libs.androidx.room.compiler)`, kotlinx-serialization-json 1.9.0, `room.schemaLocation=$projectDir/schemas`) — zero build-file edits needed. Schema v1 exported to `app/schemas/com.scholiast.android.data.db.AppDatabase/1.json`.
  - **Build-config gap (orchestrator, NOT my files):** `:app:test*DebugUnitTest` cannot compile the full test source set — Task 12's `TranscriptClientTest.kt` imports `okhttp3.mockwebserver`, which is not in the version catalog (Task 12's LOG already flagged this). I verified my tests by temporarily relocating that one file, running, and restoring it (tree bit-identical). The catalog needs `mockwebserver:4.12.0` (test classpath) to unblock `testDebugUnitTest` for everyone. I did not touch the catalog per task instructions.
  - kotlinx emits Doubles as `1.0` while JS emits `1` — fixed with `JsDoubleSerializer` (integral/finite/<1e21 → encodeLong), applied to every Double field incl. `List<Double>` type args. This is what makes the exact-string acceptance tests meaningful.
  - Field emission order = Kotlin declaration order; my declarations mirror the TS interface order exactly (frame before markup before notes; `FrameImage` = dataUrl, driveId, w, h).
  - Room `@TypeConverter`s apply to query-result POJO fields — used for the `@Transaction` page-load returning `LoadedVideoPage` with parsed `items`/`snap`.
  - Flavored test tasks: `testDevDebugUnitTest` / `testProdDebugUnitTest` (no plain `testDebugUnitTest`).
- **Decisions made (DTO contract — read before touching):**
  - JSON config `ScholiastJson`: `encodeDefaults=true` (TS always writes `notes:[]`, `tombstones{}`), `explicitNulls=false` (TS omits undefined), `ignoreUnknownKeys=true`.
  - **TS↔Kotlin naming bridges:** markup `VideoStroke/VideoLine/VideoText/VideoRect/VideoArrow` → `Stroke/Line/TextLabel/Rect/Arrow`; PageRecord's `Highlight`/`Stroke` → `PageHighlight`/`PageStroke` (unknown-field-preserving); `PageRecord` class → `VideoPage` with `typealias PageRecord = VideoPage` (task.md's name + merge-port convenience). `excalidrawScene: JsonElement?` preserved verbatim; `ocrText: String?` app-only additive at the end of VideoItem.
  - `PageHighlight`/`PageStroke`/`PageDiagram` carry `extras: JsonObject` via `ExtrasPreservingSerializer` — TS `[k: string]: unknown` survives Kotlin round-trips (verified: parse(re-encode) == parse(original) with xpath/groupId/anchor/width/points extras). Task 17's merge port MUST use `.copy(notes=…)`/`extras` so desktop highlight data is never stripped.
  - Room: `itemsJson`/`snapJson` raw TEXT columns + `JsonTypeConverters`; `SyncMetaEntity` generic key-value (`tag_index` lives there as JSON array, no `#`, sorted, union-only — mirrors desktop); `LoadedVideoPage` POJO via `@Transaction` DAO query; no destructive migration fallback, schema v1 only.
  - Repository semantics mirror `video-storage.ts` exactly (upsert-by-id, stamp `updatedAt=now`, sort by `videoTime`, page row deleted when last item goes, `updateItem` no-ops on missing page, page `updatedAt` = last item mutation). Mutations are read-modify-write without `db.withTransaction` (DAOs injected per spec) — Task 17/18: wrap in `withTransaction` if lost updates ever appear.
  - `FrameImage` includes `dataUrl: String?` (desktop runtime-only field, never written — mirrors desktop strip-on-write; parse-compat for in-memory desktop items). Deviation from plan §4.2's sketch (which omits it and orders w,h,driveId) — deliberate, logged, parse-compatible.
- **Open questions:** none. (Frame JPEG deletion on deleteItem → Task 14's `data/frames/`.)
- **Progress:** All files written, `:app:assembleDebug` BUILD SUCCESSFUL (both flavors), unit tests GREEN: VideoItemSerializationTest 10/10, VideoPageSerializationTest 4/4, VideoNoteTest 9/9 (23 total, 0 failures). Exact-string tests prove frame/transcript/note items serialize to the JSON the TS types produce (integral doubles as ints, optional fields omitted, `notes:[]` present); note parse/format round-trips incl. `withEditedMark` id-preservation; unknown-field preservation proven by parse-equality.