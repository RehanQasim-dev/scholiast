# 17-drive-sync-engine — Agent log

Log entries appended while working on this task (see README.md, "Agent logging protocol").

## [2026-08-19 22:10] task-17 agent (session 1 — recon)
- **What I learned:**
  - Task 02 DTOs are done and byte-compatible: `VideoPage` (+ `PageRecord` typealias), `PageHighlight`/`PageStroke`/`PageDiagram` with `ExtrasPreservingSerializer` (unknown desktop fields preserved verbatim), `VideoItem` (kind/videoTime REQUIRED — no defaults), `PageTombstones` (5 maps, TS order), `ScholiastJson` (encodeDefaults=true, explicitNulls=false, ignoreUnknownKeys=true). Room row `video_pages` already carries `snapJson` (merge base), `fileId`, `headRevisionId` (pagemeta) — the desktop's `snap:<url>`/`pagemeta:<url>` folded into one row. `VideoPageDao.updateSyncMeta` exists but I need full-row upsert for items+snap together → will write my own `RoomPageStore` adapter over `VideoPageDao` (constructor-injected, same pattern as Task 02's `RoomVideoItemRepository`).
  - Task 14's `FrameStore` (ui/frame) is pure java.io suspend — usable directly by the engine for frame JPEG push/pull.
  - Task 16 is NOT STARTED — `domain/sync/drive/` doesn't exist. **Decision:** I define the narrow consumer contract `DriveApi` in MY package (`domain.sync.DriveApi`) inside `SyncEngine.kt`; Task 16's `domain/sync/drive/DriveApi.kt` must implement it (their task.md already specs `updateFile(..., ifMatchRevision)` — matches my CAS design). Hand-off noted for orchestrator.
  - Desktop engine semantics pinned: serialize all ops (`chain` promise → my `Mutex`); targeted push (`syncChanged`) reconciles given URLs UNCONDITIONALLY (no isPageInSync gate); full reconcile lists `pages/` (change manifest), downloads remote-only files once to learn their url, skips in-sync pages via `isPageInSync` (fileId+headRevisionId match AND local entityFingerprint == snap fingerprint, tombstones+deletedAt excluded from fingerprint); `syncPage` retries ≤4 with fresh `findInFolder` re-merge on remote move and on local-changed-during-IO; last attempt force-writes.
  - **Critical design decision (foreign-category passthrough):** the app owns only `videoItems`. If local assembled with empty highlights/drawings/diagrams, the 3-way merge would TOMBSTONE every desktop highlight on first sync. Per the plugin's `foreign` bucket concept, my `assembleLocalPage` SEEDS highlights/drawings/diagrams from `snap` (the merge base) — l==b for those categories, so the app faithfully passes desktop edits through (remote-new kept, remote-delete tombstoned, tombstone-not-resurrected) while only ever mutating videoItems locally. The merge port itself stays byte-identical to TS.
  - Golden-test byte-equality trick: TS `JSON.stringify` emits object keys in parse/insertion order, kotlinx emits data-class field order. Byte-equal output requires fixture JSON keys ordered like the Kotlin classes (id, updatedAt, notes, color, …extras) — then both sides emit identically. Fixtures are therefore written as JSON strings in class order and fed VERBATIM to both the real TS `mergePageRecord` (node, `--experimental-strip-types`, Node v23 available) and the Kotlin port. Also: JS reorders integer-like object keys numerically (JSON.stringify) — golden tombstone/entity ids use non-integer-like ids (`h1`, `d1`…), never `"1"`.
  - VideoItem fixtures must include required `kind`+`videoTime` (TS doesn't require them) — same JSON on both sides, harmless.
- **Decisions made:**
  - Files: `domain/sync/merge/{PageRecord, MergePageRecord, PageFileName}.kt`, `domain/sync/{SyncEngine, RoomPageStore}.kt`, tests `domain/sync/merge/MergePageRecordTest.kt` + `domain/sync/SyncEngineTest.kt`, golden resources under `src/test/resources/com/scholiast/android/domain/sync/merge/`.
  - Engine keeps a row for every reconciled page (even empty items) so snap/pagemeta bookkeeping survives — mirrors desktop where snap/pagemeta outlive the va: shard.
  - `syncChanged` (targeted push) and `syncAll` (full reconcile) are the two public entries, serialized by an engine-wide `Mutex` (desktop's `chain`); per-page parallelism is Task 18's concern.
  - On 4 exhausted retries the engine throws (desktop returns silently) — Task 18's worker needs a signal to reschedule; logged as deliberate deviation.
- **Open questions:** none blocking.
- **Progress:** recon complete; task claimed. Next: merge port + fixtures + node golden generation.

## [2026-08-20 00:50] task-17 agent (session 2 — implementation + verification)
- **What I learned:**
  - Golden fixtures pre-existed; I verified them against the REAL TS `mergePageRecord` via node (`/tmp/opencode/gen-golden.mts`, Node v23.11.1 `--experimental-strip-types`): all 11 cases in `merge_page_record_fixtures.json` produced exactly the recorded `expectedJson` in `merge_page_record_expected.json` (regenerated byte-identical as golden truth).
  - Task 18 has since landed `SyncRunner`/`SyncRunSource` + `SyncGraph` — **already consuming my API**: `SyncRunner(engine: SyncEngine, drive: DriveApi, pageStore: PageStore)` drives `engine.syncPage`/`engine.isPageInSync` per page with progress, `SyncGraph.engineFactory` defaults to `UnwiredSyncRunner` until wiring lands. My public `syncPage`/`isPageInSync` signatures (already public) match; no changes needed.
  - Task 16 is mid-flight: their `domain/sync/drive/` files (`DriveOAuth.kt`, a new `drive/DriveApi.kt`) intermittently break `compileDevDebugKotlin` (same errors each time — they're actively editing). Their broken files are NOT mine to touch; my verification runs were taken in their green windows.
  - Bug caught in my own port before first run: TS reassigns `tombs.highlights = hRes.tombs` per category (mergeKeyed copies its input) — my final `PageTombstones` initially used the un-mutated inputs for highlights/drawings/videoItems/diagrams; fixed to use the merge RESULTS (`hRes.tombs` etc.); `cmTombs` alone is mutated in place (mergeNotes writes scoped tombstones straight into it) — matches TS.
  - MergePageRecordTest initially failed to compile: `JsonPrimitive.long` is an extension property (needs import) and reified `decodeFromJsonElement<T>(element)` is a top-level extension — switched to member calls (`content.toLong()`, `decodeFromJsonElement(VideoPage.serializer(), element)`).
- **Decisions made:**
  - Engine failure semantics (deviations from desktop, per session-1 plan, now concrete): (1) after 4 retries the engine THROWS (desktop returns silently) — `SyncConflictException` from the last attempt propagates; post-loop `PageSyncException` is a belt-and-braces unreachable (attempt 3 always force-writes). (2) `syncChanged`/`syncAll` aggregate per-page errors in `SyncResult.errors` and return (desktop aborts the whole run on the first error) — one bad page can't wedge the library. (3) Staleness baseline (`localBefore`) is captured BEFORE `pushImages` — the desktop compares after its own driveId stamping, which pollutes the comparison and causes spurious retries (up to 4 duplicate frame blob uploads).
  - Task 16 hand-off (recorded in `DriveApi.kt` kdoc): Task 16's `OkHttpDriveApi` must implement my `domain.sync.DriveApi` (their test proves the method shapes); their `DriveException.Conflict` maps to `SyncConflictException` in the adapter (the engine catches it for the 412 → pull → merge → re-PUT path).
- **Open questions:** none — Task 16's in-flight compile breakage will resolve with their task.
- **Progress:**
  - Wrote: `domain/sync/merge/PageFileName.kt` (wraps `Normalize.pageFileName`), `domain/sync/merge/MergePageRecord.kt` (full verbatim port: mergeKeyed, mergeNotes with `<!--timestamp:N--><!--edited:M-->` markers, jsParseInt fidelity, `TOMBSTONE_RETENTION_MS` 30d GC, insertion-ordered `kept`), `domain/sync/DriveApi.kt` (interface + `DriveFolder` + `SyncConflictException` + `PageSyncException`), `domain/sync/PageStore.kt`/`RoomPageStore.kt` (PageSnapshot + Room impl over `VideoPageDao`), `domain/sync/SyncEngine.kt` (`syncChanged` unconditional targeted push · `syncAll` list-as-change-manifest + remote-only discovery + `isPageInSync` zero-network skip · `syncPage` ≤4 attempts with CAS pre-check + 412 re-merge + mid-IO-edit re-merge + frame push/pull · engine-wide Mutex), tests `merge/MergePageRecordTest.kt` (golden, byte-for-byte vs TS) + `SyncEngineTest.kt` (11 tests: targeted push isolation, in-sync skip with zero network, both-devices comment merge, tombstone no-resurrect, local re-add resurrect, frame upload-once, frame pull, remote-only discovery, CAS 412→pull→merge→re-PUT, mid-IO edit pickup, 4-attempt bounded failure).
  - Verified: `./gradlew :app:testDevDebugUnitTest` with both my test classes → **all 12 pass** (golden + 11 engine). Full-suite runs green except Task 18's `SyncStatusRepositoryTest`/`SyncSchedulerTest` (their files, their task) and Task 16's in-flight compile errors.
  - Task status → DONE.