# 13-transcript-panel-annotate — Agent log

Log entries appended while working on this task (see README.md, "Agent logging protocol").


## [2026-08-19 21:40] task-13 agent (RESUME — session 1, fresh start)
- **What I learned:**
  - Task 02 DTOs: `VideoItem(kind:"transcript", videoTime, timeEnd, quote, color, anchor: TranscriptAnchor{startCue,startOffset,endCue,endOffset}, notes[])` — anchor offsets are PER-CUE char offsets (desktop `video-transcript-panel.ts` `offsetWithinCue`); `videoTime` = start of start cue, `timeEnd` = end of end cue (seconds; Android cues carry ms).
  - Task 12 client: `getTranscript(videoId, preferredLang?)` → `TranscriptResult.Success(LoadedTranscript{videoId, languageCode, tracks, cues, paragraphs})`; `setSessionLanguage(videoId, code)` (per-video session); `pickTrack` prefers session pref → English non-ASR → first. `TranscriptParagraph(index, text, startMs, endMs, cueRange: IntRange)` — text = cue texts joined " ".
  - Task 06 contract to reuse: `VideoTimeProvider.currentTime()`, `SeekRequestListener.seekTo(Double)`, `EditorDraft(itemId?, videoTime, text)`, `CommentEditorSheet(draft, timestampSeconds, onSave, onCancel, seekListener)` (still the Task-06 placeholder; Task 07 has NOT landed its sheet — its `ui/notes/editor/EditorViewModel.kt` exists but is mid-flight), `TimestampChip`, `formatVideoTime`, `genVideoId`.
  - Compose resolves ui 1.9.0 (BOM 2025.06.01 → 1.8.3 but material3/lifecycle bump ui-text/foundation to 1.9.0): `SelectionContainer(modifier, selection, onSelectionChange, content)` overload exists; `LinkAnnotation.Clickable(tag, TextLinkStyles, linkInteractionListener)` + `builder.pushLink` (Task 08 pattern); `AnnotatedString.annotations` holds links in 1.9 (no separate `linkStyles`).
  - Desktop `.ob-vt-hl` = 0.40 alpha background on yellow/red/green; active cue `.is-now` = bigger/brighter text.
  - **Build environment (IMPORTANT):** Task 11's NDK/CMake config now breaks `:app:assembleDebug` on this machine — no NDK installed (`~/.bubblewrap/android_sdk` has none; `sdkmanager` CLI is broken). Workaround verified: fake `ndk/28.2.14247001/source.properties` (Pkg.Revision) + `-x` the CMake/native/strip tasks. Additionally the Kotlin compile is RED on 6 files owned by Tasks 07/09/10/11 (`domain/transcribe/{FutoTranscriber,Transcriber}.kt`, `domain/voice/local/{ModelDownloader,Models,WhisperGGML}.kt`, `ui/notes/editor/EditorViewModel.kt`). Per the task rules I'll verify with those moved aside, byte-identical restore.
- **Decisions made:**
  - Panel owns the comment-editor sheet opening (creation surface); highlight tap = seek to range start + open the sheet for that item; thread RENDERING stays Task 06's Notes tab cards (Task 06's `NoteItemCard`). Logged per task.md agent note.
  - Selection model: per-paragraph `SelectionContainer` (offsets within the paragraph's joined text), pure `mapParagraphRange` maps paragraph-text range → (cueIndex, per-cue offsets) + trimmed quote; tap = whole-paragraph selection (plan §5.6.3 stylus-friendly).
  - Live follow port: 250 ms poll via `VideoTimeProvider` (desktop setInterval), VM early-returns when the active cue hasn't changed (state write only on change); auto-scroll only when the active paragraph leaves the 10%..80% band → animate to 30% from top; initial scrollToLookback = cue at (now − 30 s) at 25%.
  - Swatch popup = port of desktop `showPopup` (above selection, flip below; 6dp clamps). Repaint = `AnnotatedString` background spans at 0.40 alpha + `LinkAnnotation.Clickable` per highlight (tap → seek + comment).
  - VM takes a `TranscriptProvider` interface (adapter wraps Task 12's concrete `TranscriptClient`) so unit tests use a fake — no mocking lib.
- **Open questions:** none blocking.
- **Progress:** task.md → IN PROGRESS. All references read; about to write `ui/transcript/TranscriptViewModel.kt`, `TranscriptSelectionOverlay.kt`, `TranscriptPanel.kt`, `TranscriptPanelTest.kt`.

## [2026-08-19 18:40] task-13 session 2
- **What I learned:**
  - Compose foundation 1.9.0 (BOM 2025.06.01) keeps `SelectionContainer(modifier, selection, onSelectionChange, …)` **internal** — the controlled-selection overload cannot be used; only the public no-selection `SelectionContainer(content)` exists. Also `TextSelectionColors.selectionColor` was renamed → only `backgroundColor` + `handleColor` exist (`LocalTextSelectionColors.current.backgroundColor` is the selected-text fill).
  - `TextLayoutResult.getBoundingBox` is **per-offset only** (no `IntRange` overload in 1.9) — merged the first/last glyph boxes for a range rect.
  - `SpanStyle.background` is a non-nullable `Color` (default `Color.Unspecified`) — filter `!= Color.Unspecified`, never `!= null`.
  - `TranscriptChunker.chunk` splits sentences: my 4-cue fixture ("The first sentence"/"ends here."/"A second thought?"/"Certainly.") chunks into **3** paragraphs, not 2.
  - `const val` cannot hold a `ClosedFloatingPointRange` (`FOLLOW_BAND`) — plain `val`.
  - Desktop `cueInnerHtml` overlap rule (verified against `video-transcript-panel.ts:227-235`): `segs.sort(by start)` + `if (seg.start < pos) continue` — the **earlier-starting** segment keeps its paint; overlapping later ones are dropped entirely. (The desktop's own comment says "later item wins" but the code does the opposite; the port follows the code.)
  - `FutoTranscriberTest.kt` (Task 09's test) fails to compile on this branch (`Cannot infer type for type parameter 'T'` at :265, `Unresolved reference 'cancel'` at :285) — pre-existing, not mine; I temporarily moved it to `/tmp/opencode/FutoTranscriberTest.kt.bak` for my verification run and restored it byte-identical. Task 09 should fix it.
- **Decisions made:**
  - **Custom drag selection instead of SelectionContainer:** each paragraph gets `pointerInput` + `detectDragGestures` mapping pointer → text offset via `TextLayoutResult.getOffsetForPosition` (`change.consume()` so the list doesn't scroll mid-drag). Drag on a paragraph selects; scroll via gaps/padding, scrollbar, or live-follow. Tap = whole-paragraph selection (unchanged). Live drag range is painted with `LocalTextSelectionColors.current.backgroundColor` overlaying saved highlights; the popup rect now reads the real drag range (the earlier `selections` map / `Selection?` plumbing was removed because the controlled overload is internal).
  - Swatch popup for a tap anchors over the whole paragraph; for a drag over the merged first/last glyph boxes (multi-line drags span the whole wrapped block — acceptable).
- **Open questions:** none blocking.
- **Progress:** wrote all four owned files. `TranscriptViewModel.kt` (TranscriptStatus, PendingSelection, TranscriptUiState, pure helpers `activeCueIndex`/`lookbackCueIndex`/`paragraphIndexForCue`/`paragraphCues`/`cueStartOffsetInParagraph`/`mapParagraphRange`/`formatRangeLabel`, `TranscriptProvider` + `ClientTranscriptProvider`, VM: load/refreshHighlights/changeLanguage/onTick/onParagraphTap/onParagraphSelection/clearSelection/createHighlight/addReply/seekTo/onHighlightTap/pendingRangeLabel/tracks), `TranscriptSelectionOverlay.kt` (`highlightColor`, `ColorSwatch` 44dp, popup with flip-below + 6dp clamps), `TranscriptPanel.kt` (`buildParagraphAnnotated` with 0.40-alpha spans + LinkAnnotation.Clickable + live-drag span + active-cue Medium weight; panel with 250 ms follow, lookback scroll with viewport retry, band auto-scroll with line-exact 30% target, per-paragraph drag selection, popup, CommentEditorSheet wiring, `transcriptViewModel(url)` factory), `TranscriptPanelTest.kt` (25 tests: anchor math incl. join-space boundary + whitespace/out-of-bounds/inverted rejection, follow/lookback index math, `M:SS–M:SS` labels incl. `H:MM:SS`, default-language pick, VM load/error/tick-early-return/highlight-save shape/addReply stamping/language switch/refresh/seek, repaint spans + overlap-skip + active-cue weight).
  - **Verified:** `./gradlew :app:testDevDebugUnitTest` — BUILD SUCCESSFUL, 25/25 transcript tests pass (whole suite 288+ tests, 0 failures with Task 09's file aside); `./gradlew :app:assembleDebug` — BUILD SUCCESSFUL. task.md → DONE.
