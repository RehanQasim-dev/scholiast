# LOG — task-26-linearizer

## [2026-08-21 00:00] task-26 agent
- **What I learned:** `LinearArticle`/`LinearBlock`/`LinearAnn` (Task 23) already exist in `data/model/LinearArticle.kt`; `LinearAnn.target` is non-null and `""` for non-link annotations (matches Task 23's serialization test). Jsoup is visible on the main source set purely transitively from readability4j — no explicit jsoup dependency line was needed.
- **Decisions made:**
  - **Block-text normalization contract for Tasks 24/28/29:** every text block has whitespace runs collapsed to single spaces and ends trimmed (NBSP `\u00A0` treated as collapsible). Exception: `pre` → `"code"` blocks keep text **verbatim** via `Element.wholeText()` so indentation survives; they carry no annotations. `code` appearing inline is an annotation (`kind="code"`), not a block.
  - Annotations are recorded against the raw concatenated text, then remapped onto the cleaned text through a raw→clean index map built during collapsing; annotations that collapse to zero length are dropped; list sorted by (start, end).
  - Nested lists split the parent `li`: each li emits its own "li" block, nested ul/ol emit their li blocks immediately after (document order). No bullet prefix baked into text.
  - Images inside inline content are deferred and emitted as their own "img" blocks right after the surrounding text block. `imgUrl` = `abs:src`, falling back to the first `srcset` candidate resolved absolute; `srcset` beyond first candidate ignored per spec.
  - Truncation valve reads literally ("stop once cumulative chars exceed maxChars"): the crossing block IS included, then emission stops and `truncated=true`. So total ≤ maxChars + last block. wordCount counts whitespace-separated tokens across emitted blocks only.
  - Stray inline content between block children (e.g. bare text in a div) becomes an implicit "p" block so no visible text is lost.
- **Open questions / deviations:**
  - Mid-session, `assembleDevDebug` failed compiling `domain/reader/Extractor.kt` (Task 25's file, untracked WIP, task marked NOT STARTED) — I did not touch it per ownership rules; verified my code+tests green by compiling Linearizer.kt + LinearArticle.kt + test in isolation with the cached kotlin-compiler-embeddable. The parallel Task 25 agent then fixed its file and the official Gradle build went green. No edits outside my two owned files at any point.
- **Progress:** Implemented `domain/reader/Linearizer.kt` (exact spec signature, default `maxChars=400_000`) + `LinearizerTest.kt` with the 6 mandated fixtures (mixed formatting + skipped chrome, nested lists, images/srcset/figcaption, partial-span link offsets, truncation valve, round-trip-every-sentence-exactly-once). `./gradlew assembleDevDebug` BUILD SUCCESSFUL; `./gradlew testDevDebugUnitTest --tests "...LinearizerTest"` → tests=6, failures=0, errors=0.
