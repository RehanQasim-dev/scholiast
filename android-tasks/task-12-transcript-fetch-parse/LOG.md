# Task 12 — Transcript fetch & parse — work log

## [2026-08-19 18:21] task-12-agent
- **What I learned:**
  - Task 01 (Gradle skeleton) is **NOT STARTED** — `android/` contains only `AGENTS.md` and an empty `src/`. No version catalog, no Gradle wrapper. I own exactly 5 files: `domain/transcript/{TranscriptModels,CueParser,TranscriptChunker,TranscriptClient}.kt` (main) + `TranscriptClientTest.kt` (unit test).
  - Desktop sources read: `video-transcript.ts` (innertube player call, `pickTrack`, `parseCuesXml`, `semanticChunk`) and `yt-transcript-extractor.ts` (request shapes: `POST youtubei/v1/player?prettyPrint=false`, body `{"context":{"client":{clientName,clientVersion}},"videoId":...}`, IOS `20.10.3` then WEB `2.20240101.00.00`; track fetch = `baseUrl` plain XML on desktop).
  - Desktop `semanticChunk` flush rules (exact): gap between **consecutive** cue starts > 20 s; sentence end on current cue (`[.!?。！？]["')\]’”]?\s*$`); accumulated span from paragraph start ≥ 30 s (flushed at cue boundary, only if the sentence rule didn't just fire). `splitOnInternalSentences` exists in the TS but `loadTranscript` never calls it (documented as "handled upstream") — I ported both, exposed separately, so behavior can match the TS byte-for-byte.
  - Desktop `pickTrack` order: session pref (exact `languageCode` match, ASR not deprioritized) → English (`startsWith('en')`, non-ASR preferred) → first (non-ASR preferred).
  - Desktop `parseCuesXml` is srv3 `<p t="ms" d="ms"><s>…</s></p>` with a `<text start="s" dur="s">` fallback; cue end = start+dur. Desktop `loadTranscript` overwrites ends with "next cue start, or +5 s" — Android uses real `dDurationMs` from JSON3 instead (no +5 hack needed).
  - Cue anchoring per plan §5.6.3 is `(startCue, startOffset, endCue, endOffset)`; panel keeps per-cue spans inside paragraphs, so paragraphs must retain per-cue text/timing, not a single blob.
- **Decisions made:**
  - **Cue index = position of the `tStartMs` event in the JSON3 `events` array** (task spec: "stable integer index (position in the JSON3 events array)"). aAppend events never create cues; indexes can therefore be non-contiguous (e.g. [0,2,3,…]). XML-fallback path uses emitted position (`cues.length`), exactly like the TS.
  - **JSON3 is the primary track format** (`baseUrl&fmt=json3`, task spec). `CueParser.parse()` auto-detects: body starting with `<` → XML fallback (defensive, in case `fmt=json3` is ignored); else JSON3. XML fallback is a faithful `parseCuesXml` port.
  - **`TranscriptResult`** = sealed interface: `Success(LoadedTranscript) | NoCaptions | HttpError(statusCode) | NetworkError(cause) | ParseError(message, cause)`. 404 is just `HttpError(404)` — UI checks the code.
  - `TranscriptParagraph` = `(index, text, startMs, endMs, cueRange: IntRange)` — `text` is the cue texts joined with `" "` (per-cue `text`/`startMs`/`endMs` remain on `TranscriptCue` for offset anchoring and live follow); `cueRange` spans first..last cue index (covers aAppend gaps).
  - `TranscriptClient` is a stateful class (OkHttp injectable, `playerEndpoint` injectable) holding the desktop's three session maps (tracks cache, per-`videoId:lang` transcript cache, `sessionLangPref`). `setSessionLanguage(videoId, code)` sets the pref; picker flow = `setSessionLanguage` + `getTranscript(videoId, code)`. Per-video session semantics match the desktop page-session.
  - Innertube response walked defensively via kotlinx JsonElement (`as?` safe casts) — no strict DTOs, since the response shape drifts. No `@Serializable` classes → no serialization compiler plugin needed.
  - **No Gradle exists** → I'm verifying by downloading kotlinc 2.0.21 + kotlinx-serialization-json 1.7.3 + okhttp/okio + mockwebserver 4.12.0 + junit 4 into `/tmp/opencode`, compiling main+test with plain `kotlinc`, and running via `org.junit.runner.JUnitCore`. When Task 01 lands, the test needs `mockwebserver` + `junit` on the test classpath (version catalog note below).
- **Open questions:**
  - None blocking. Note for Task 13: `chunk()` (split-then-group) vs `semanticChunk()` (exact TS port) differ on cues containing two sentences — pick deliberately.
- **Progress:**
  - Read all reference material (plan §2/§5.6.1–3/§12, both TS sources, android/AGENTS.md).
  - Started: task.md → IN PROGRESS; toolchain download in flight.
## [2026-08-19 18:31] task-12-agent
- **What I learned:**
  - **Exact innertube request/response shapes used (recorded per task spec):**
    - Request: `POST https://www.youtube.com/youtubei/v1/player?prettyPrint=false`, header `Content-Type: application/json`, body `{"context":{"client":{"clientName":"IOS","clientVersion":"20.10.3"}},"videoId":"<id>"}`; fallback context `{"client":{"clientName":"WEB","clientVersion":"2.20240101.00.00"}}` (both ported verbatim from `video-transcript.ts`; the ANDROID-client variant with UA header is dropped — native app, no forbidden-header problem, but IOS→WEB is the desktop's primary path and we stay byte-compatible with it). `Accept-Language: <lang>` sent when a language is in play (desktop behavior).
    - Track fetch: `GET <track.baseUrl>&fmt=json3` (appended with `&` since baseUrl always carries query params). Response = JSON3 `{wireMagic:"pb3", pens, wsWinStyles, wpWinPositions, events:[{tStartMs, dDurationMs, segs:[{utf8, tOffsetMs?, acAsrConf?}]}, {aAppend:<idx>, segs:[...]}, ...]}`.
    - Player response shape read: `captions.playerCaptionsTracklistRenderer.captionTracks[]`, entry = `{baseUrl, languageCode, kind?:"asr", name:{simpleText}|{runs:[{text}]}}`. Tracks without `baseUrl` are dropped (desktop behavior).
  - **kotlinc 2.0.21 does not run on JDK 25** (`JavaVersion.parse` IllegalArgumentException) — used `/usr/lib/jvm/java-17-openjdk-amd64`. Gradle-less verification works: plain `kotlinc -classpath` compile + `org.junit.runner.JUnitCore` run.
  - kotlinx.serialization `longOrNull`/`contentOrNull` are package-level extensions — need explicit imports (I initially imported only the class names; explicit-import mistake cost one compile round).
  - Test-writing gotchas found by the fixtures (all were wrong expectations in MY test, not port bugs — the port matches TS exactly):
    - The 30 s max-group rule flushes **after** pushing the triggering cue, so the trigger belongs to the flushed paragraph (desktop `pending.push(c)` happens before the max check). Same for the sentence rule (cue ending a sentence flushes with itself).
    - The 20 s gap rule flushes **before** pushing (desktop checks `prev` before push).
    - All-ASR track list: English ASR beats the first non-English track (`en` wins over `es`).
    - `chunk()` (split-then-group) ≠ `semanticChunk()` on two-sentence cues: the fixture's 6 cues → 6 paragraphs (semanticChunk, exact TS port) but 8 paragraphs (chunk, what the client serves).
- **Decisions made:**
  - Client serves `TranscriptChunker.chunk(cues)` (internal-sentence split + semanticChunk) — the pipeline the TS comments document as intended. `semanticChunk` is exposed and unit-tested as the byte-exact port for the "matches TS behavior" acceptance criterion. Task 13 can switch freely; both are one call.
  - The three session maps (tracksCache / transcriptCache / sessionLangPref) live on the client instance (app-session scope). `fetchTracks` exposes the full track list for the picker; `setSessionLanguage(videoId, code)` sets the pref and the picker then calls `getTranscript(videoId, code)` (cache per `videoId:lang`, so switching back is instant).
  - `TranscriptClient` takes an injectable `OkHttpClient` + `playerEndpoint` (tests point it at MockWebServer; no prod test-seams needed — the caption baseUrl fixture is built from `server.url(...)` at test time).
- **Open questions:**
  - None.
- **Progress:**
  - Wrote all 4 production files + the test file. Compiled with kotlinc 2.0.21 (JDK 17) — zero errors.
  - **Test run: 24/24 OK** (JUnitCore, 0.77 s): JSON3 aAppend parsing (6 cues, indexes [0,2,3,4,6,7], accumulated texts, ms timings), JSON3 no-events/malformed cases, srv3 + text XML formats with entity decoding, parse auto-detect, semanticChunk on fixture (6 paras) + gap rule + 30 s rule, splitOnInternalSentences, pickTrack priority (5 cases), MockWebServer: happy path (IOS context + `fmt=json3` + `lang=en-orig` asserted on the wire), IOS→WEB fallback (context names asserted on the wire), NoCaptions (2 player calls, no caption fetch), HttpError 404 (player + caption), ParseError (malformed caption body), NetworkError (dead port), session-language default + per-language cache, fetchTracks picker list.

## [2026-08-19 18:32] task-12-agent — FINAL SUMMARY
- **Done.** Files written (all under `android/app/`):
  - `src/main/java/com/scholiast/android/domain/transcript/TranscriptModels.kt` — `TranscriptCue(index, startMs, endMs, text)`, `TranscriptParagraph(index, text, startMs, endMs, cueRange: IntRange)`, `CaptionTrack(languageCode, name, baseUrl, isAsr)`, `LoadedTranscript`, sealed `TranscriptResult` (Success / NoCaptions / HttpError(statusCode) / NetworkError(cause) / ParseError(message, cause)).
  - `src/main/java/com/scholiast/android/domain/transcript/CueParser.kt` — pure JSON3 parser (aAppend accumulation, cue index = events-array position, endMs from `dDurationMs`) + faithful `parseCuesXml` port (srv3 + `<text>`, entity decode) + auto-detect; `ParseException` for typed parse failures.
  - `src/main/java/com/scholiast/android/domain/transcript/TranscriptChunker.kt` — `semanticChunk` (byte-exact TS port), `splitOnInternalSentences`, and `chunk()` (the split+group pipeline the client serves).
  - `src/main/java/com/scholiast/android/domain/transcript/TranscriptClient.kt` — innertube client: IOS→WEB player POST, `parseCaptionTracks` seam (defensive JsonElement walks), companion `pickTrack` (preferred→English-non-ASR→first), `baseUrl&fmt=json3` fetch, `setSessionLanguage`, `getTranscript(videoId, preferredLang)`, `fetchTracks` for the picker; session caches mirroring the desktop maps; injectable OkHttpClient/playerEndpoint.
  - `src/test/java/com/scholiast/android/domain/transcript/TranscriptClientTest.kt` — 24 fixture-based tests, all green (verified below).
- **Verification:** Task 01's Gradle does not exist yet, so per task spec I wrote everything and additionally proved it: compiled main+test with `kotlinc 2.0.21` (JDK 17) and ran `org.junit.runner.JUnitCore` → **OK (24 tests)** in 0.77 s, using MockWebServer for the innertube + caption endpoints (jars downloaded to `/tmp/opencode/t12/libs`, compile/run commands in the entry above). When Task 01 lands, add to the test classpath: `junit:junit:4.13.2`, `com.squareup.okhttp3:mockwebserver:4.12.0`, and to main: `okhttp`, `kotlinx-serialization-json`, `kotlinx-coroutines-core` (all already in the plan's stack). No `@Serializable` classes → no serialization compiler plugin needed.
- **Acceptance criteria:** ✓ parse captured JSON3 sample (cues, aAppend, timings); ✓ semanticChunk matches TS behavior on the same fixture + synthetic gap/30 s boundaries; ✓ pickTrack priority verified (preferred / English-non-ASR / first / all-ASR / empty); ✓ typed NoCaptions / HttpError 404 / ParseError / NetworkError; ✓ language-picker data via `fetchTracks`; ✓ `setSessionLanguage` re-fetch flow; optional live-video integration test intentionally omitted (network-disabled CI per task spec; fixture tests are the gate).
- **For Task 13:** use `TranscriptChunker.chunk()` for panel paragraphs (or `semanticChunk` if you want the desktop's literal output); anchors are `(cueIndex, charOffset)` where `cueIndex` is the JSON3 events-array position — non-contiguous gaps are normal; per-cue `startMs`/`endMs` support live follow and `M:SS` chips; `TranscriptResult` cases map 1:1 to UI states.
