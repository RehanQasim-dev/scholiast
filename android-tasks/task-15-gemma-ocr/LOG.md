# 15-gemma-ocr — Agent log

Log entries appended while working on this task (see README.md, "Agent logging protocol").

## [2026-08-19 23:47] agent-task15
- **What I learned:** Task 14's `OcrHook` (ui/frame/OcrHook.kt) is `fun interface { suspend fun run(itemId: String, imageFile: File): String? }`, called inline in `FrameCaptureViewModel.save()` — the hook runs BEFORE the VM sets `Saved`/resumes playback. Task 10's `Service.GEMMA` + `ApiKeyProvider.apiKey(Service)` is the key seam (Task 16 implements it with the Keystore). Task 02 ships `VideoItem.ocrText` (app-only additive field), `OcrTextEntity(itemId, text, source, createdAt)` + `OcrTextDao` (upsert REPLACE), `VideoItemRepository.updateItem(url, item)` (stamps `updatedAt` → drives sync). Gemma 4 is the current Gemma family (released 2026-04); concrete Google-AI-API model id is `gemma-4-31b-it` (AI Studio link `?model=gemma-4-31b-it`); OCR-capable, serveable via the standard `v1beta/models/{model}:generateContent?key=` REST endpoint with `inlineData` image parts (same shape GeminiTranscriber uses for audio).
- **Decisions made:**
  - Files per task.md: `domain/ocr/GemmaClient.kt`, `domain/ocr/OcrRunner.kt`, `domain/ocr/OcrStorage.kt`, `domain/ocr/GemmaClientTest.kt`.
  - Model id: `GemmaClient.DEFAULT_GEMMA_MODEL = "gemma-4-31b-it"` (plan says "Gemma 4", §5.11 leaves the concrete string open; Task 19's Settings should make it editable). NOTE: Task 10's `SpeechSettings` has NO `gemmaModel()` accessor — Task 19 must add one (or pass the value into `GemmaClient`/`OcrRunner` directly) to make it user-editable.
  - Plan §5.9 still lists OCR under "v1.1" — stale wording; §5.7.3 + §9 M4 confirm OCR-at-save ships with M4. Followed M4.
  - Error categories reuse Task 10's `TranscriptionError` enum (NOT_CONFIGURED/NETWORK/UNAUTHORIZED/RATE_LIMITED/INVALID_REQUEST/SERVER/UNKNOWN) instead of a parallel OCR enum; `OcrResult` = Success(text) / Skipped(NO_KEY|FILE_MISSING) / Failure(error, message, cause).
  - **Detached runner:** because Task 14's VM calls the hook inline before `Saved`+resume, a synchronous OCR (up to minutes on a big model) would delay the save state and playback resume. `OcrRunner.run` therefore LAUNCHES into an injected `CoroutineScope` (SupervisorJob + Dispatchers.IO, "low priority" = IO pool) and returns null immediately; work logs, retries once on transient failure (NETWORK/SERVER/RATE_LIMITED), log-and-drops otherwise. `detached=false` seam (tests, future sync flows). This deviates from the hook's "return the text" doc — the VM ignores the return anyway.
  - Storage: `OcrStorage.store(itemId, text)` resolves the item's page by scanning `listAllPages`+`loadPage` (the hook signature has no url — O(pages) scan, fine at OCR priority), then `updateItem(url, item.copy(ocrText=text))` (stamps updatedAt → ocrText participates in Drive sync) + `OcrTextDao.upsert(OcrTextEntity(itemId, text, "gemma", now))`. `SOURCE_GEMMA = "gemma"`.
  - Offline: `OcrRunner` takes `isOnline: suspend () -> Boolean` (default `{ true }`); Task 19 wires ConnectivityManager. Offline → logged skip, `ocrText` stays null ("OCR queued" chip on the frame card is Task 06/14 UI, not implemented here).
  - Cleanup (`cleanOcrText`): CRLF→LF, trim, strip a wrapping ```fence (+ known fence lang tokens text/txt/plaintext/markdown/md), collapse 3+ blank lines to 1 blank line. Client returns `Failure(UNKNOWN)` when the cleaned text is blank ("model returned no text") — runner's blank handling lives in the client.
- **Open questions:** none blocking. Task 19: needs `gemmaModel()` on SpeechSettings + real `isOnline` + scope wiring; Task 16 implements `ApiKeyProvider` for `Service.GEMMA` (shared key with Gemini per plan §5.11 "Gemini/Gemma key").
- **Progress:** task.md → IN PROGRESS. Reading done; files next.


## [2026-08-20 00:40] agent-task15 (CONTINUE — verification pass)
- **What I learned:** The tree is a live target: Task 16/17/18 agents were actively editing `domain/sync/`, `domain/sync/drive/`, and their tests the whole session (files appeared/changed every few minutes: SyncGraph→SyncRunner/SyncRunSource refactor at 00:29-00:30, Task 16's whole drive package dropped 00:26-00:29, MergePageRecordTest stale vs the refactored `VideoPage` model). `FrameCaptureViewModel.save()` (ui/frame/FrameCaptureViewModel.kt:153) already fires the hook: `runCatching { ocr.run(item.id, store.fileFor(item.id)) }` — Task 14's wiring is done, no changes needed there. `OcrRunner` is a drop-in `OcrHook` (fun interface, `suspend run(itemId, imageFile): String?`).
- **Decisions made (verification fixes to MY files):**
  - **OcrRunner.kt: removed the `android.util.Log` dependency.** Unit tests run against the Android stub jar, where `Log.i/w` throws "not mocked" — 9/26 tests failed. The runner now takes an injected `log: (OcrLogLevel, String) -> Unit` (default no-op), keeping the class pure JVM per AGENTS.md; Task 19's wiring should pass `{ level, msg -> if (level == OcrLogLevel.WARN) Log.w(TAG, msg) else Log.i(TAG, msg) }`. New `OcrLogLevel { INFO, WARN }` enum.
  - **GemmaClientTest.kt: 12 compile errors** — `runnerEnv { setKey(...) }` trailing lambdas bind to the LAST parameter (`scope: CoroutineScope`), not `configure` — rewrote all 6 call sites as `runnerEnv(configure = { ... })`. Also `runnerEnv` hardcoded `detached = false`, so the detached-mode test ran inline; added a `detached: Boolean = false` parameter and the detached test passes `detached = true`.
  - Restored everything I touched outside my scope byte-identical (MainActivity.kt, Task 16's drive package, Task 17/18 main+test files) — where an agent had rewritten a file mid-session, the NEWER in-tree version was kept.
- **Open questions:** none. Hand-off for Task 19: wire `OcrRunner` into `FrameCaptureViewModel` (replace `NoopOcrHook` default) with `GemmaClient(keys, ...)`, a `CoroutineScope(SupervisorJob() + Dispatchers.IO)`, a real `isOnline` (ConnectivityManager), and the Log-backed `log` fn. `SpeechSettings` still has NO `gemmaModel()` — Task 19 must add it (plan §5.11) or pass `GemmaClient.DEFAULT_GEMMA_MODEL` directly.
- **Progress (build/test results):**
  - `:app:assembleDebug` — **BUILD SUCCESSFUL** (JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64).
  - `:app:testDevDebugUnitTest` — **GemmaClientTest: 26/26 PASS** (request shape, response parse, cleaning, error mapping, no-key/missing-file skip, offline skip, retry-once policy, 401-no-retry, blank-drop, detached mode, OcrStorage upsert + cross-page find). Full suite: **363 tests, 2 failures — both Task 18's own in-flight tests** (SyncSchedulerTest + SyncStatusRepositoryTest state-flow assertions against their mid-refactor SyncStatusRepository; unrelated to OCR). When Task 16/17 files were also mid-broken, verification ran with them temporarily moved to /tmp/opencode/ and restored byte-identical afterward.
  - Task 17's MergePageRecordTest.kt is currently RED in the tree (stale vs the refactored `VideoPage` model: `Unresolved reference 'long'` + DeserializationStrategy mismatch at lines 35/57) — Task 17's agent to fix.
  - Acceptance criteria met: MockWebServer request-shape test ✓, OcrHook integration verified (VM already fires it; FrameCaptureTest 39/39 green) ✓, skip/retry unit tests ✓.
