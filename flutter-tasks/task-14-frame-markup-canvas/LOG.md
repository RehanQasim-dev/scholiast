# Task 14 — Frame Markup Canvas — Work Log

## [2026-08-22 22:10] ses_fd5559a14ffecM3xZfyzhb0QTJ (resume — Frame Markup Canvas)
- **What I learned:**
  - Prior agent left markup_math.dart (432 lines, FrameColor enum) and markup_painter.dart (253 lines) on disk with wrong import depth `../../core/models/video_item.dart` → should be `../../../` (frame is 3 deep, same as `ui/screens/player/transcript_panel.dart`). Causes `uri_does_not_exist` + undefined `Stroke/VideoMarkup` etc. and inference failures in `eraseStrokes` where lambdas were untyped.
  - painter had additional `Rect` collision: model `Rect` vs `dart:ui.Rect` — needs `as vm` prefix and `ui.Rect.fromLTWH` for preview shape.
  - `VideoMarkup`/`FrameImage` fields exactly as in `lib/core/models/video_item.dart:330-458` (points normalized 0..1, rects/arrows optional, FrameImage w/h + optional dataUrl/driveId). `PlayerStateNotifier.addFrameCapture({required FrameImage frame, VideoMarkup? markup})` is the Riverpod integration point; wrapper should call `playerStateNotifierProvider(pageUrl).notifier`.
  - Test pattern: `AppDatabase.inMemory()` + `ProviderScope(overrides: [databaseProvider.overrideWithValue(db)])` (see `test/ui/player_screen_test.dart`, `database_test.dart`). Toolbar in 800×600 test is scrollable — `Icons.undo` chip may be off-screen, so undo should be tested via Ctrl+Z key event not tap. Eraser hit-testing is point-to-segment (not segment-to-segment) — eraser path needs points near stroke to hit.
- **Decisions made:**
  - Fixed `markup_math.dart:3` import depth; added explicit types `(Stroke s)`, `(Line l)`, `(Rect r)`, `(Arrow a)`, `(TextLabel t)` in `eraseStrokes` where clauses and braced single-line `if(... ) return false` to satisfy lint + fix `argument_type_not_assignable`. Changed `final th = 22.0` to `const`.
  - Fixed `markup_painter.dart` to `import '../../../core/models/video_item.dart' as vm`; qualified all model types (`vm.VideoMarkup`, `vm.Rect`, `vm.Arrow`, `vm.TextLabel`) and kept `Rect.fromLTWH` as `ui.Rect` via prefix. Kept `FrameColor` unprefixed from `markup_math.dart`.
  - Wrote `lib/ui/screens/frame/frame_draw_screen.dart` (~750 lines): dark `Color(0xFF0B0D14)` scaffold + `Color(0xFF8B7CF6)` accent; `enum MarkupTool {pen,highlighter,line,rect,arrow,text,eraser}`; `FrameDrawScreen` StatefulWidget with `FrameImage frame`, `ui.Image? frameImage`, `VideoMarkup? initialMarkup`, `onSave(FrameImage,VideoMarkup?)`/`onCancel`; gesture → normalized via `MarkupMath.normalizeFlattened`/`normalize` using `frame.w/h`; `MarkupSession` for undo/redo (cap 50); tool bar with ChoiceChip tools, 4 circular color chips, undo/redo/clear, Discard + Save (Enter) FilledButton; keyboard via `Focus(onKeyEvent:)` handling Esc (cancel), Enter (save), Ctrl+Z / Ctrl+Shift+Z / Ctrl+Y (undo/redo); pen/highlighter → committed `Stroke` with filtered `filterMinDistance`; line/rect/arrow → shape anchors `_shapeStart/_shapeEnd` committed via `pushSnapshot` + `replace`; text → `_showTextDialog` at tap anchor with `TextField` → `TextLabel`; eraser → `eraseStrokes` with `tol 12px`; in-progress preview via `_PreviewPainter` (pen/highlighter smoothed quadratic path, eraser white overlay, shape preview); save composites via `renderMarkupToJpeg` (PictureRecorder) when `frameImage` + markup present, then `await onSave(outFrame, markup)`; `FrameDrawScreenRiverpod` ConsumerWidget wiring `playerStateNotifierProvider(pageUrl).notifier.addFrameCapture`.
  - Wrote `test/ui/frame_markup_test.dart` (19 tests): MarkupMath normalize/denormalize, flatten/unflatten/normalizeFlattened, filterMinDistance, distToSegment, eraserHits (corrected eraser points near stroke), weightFor/renderWeightPx, FrameColor.fromJson, MarkupSession commit/undo/redo + cap, eraseStrokes, PalmRejection; MarkupPainter strokePath + two widget smoke tests (with/without frameImage PictureRecorder image); FrameDrawScreen drag→stroke + save callback normalized JSON round-trip, undo via Ctrl+Z before save, Esc cancel, color picker smoke, Riverpod wrapper with `AppDatabase.inMemory` + `ProviderScope`.
  - Verified `flutter analyze` now 0 errors (23 infos/warnings only, none in owned files after fix). Fixed two test failures: eraser path dense near stroke, undo via key event instead of off-screen tap.
- **Open questions:**
  - `frame_bytes` persistence still assumes caller handles FrameStore file IO; Riverpod wrapper currently passes `FrameImage(w/h)` metadata only — file write could be added in `FrameStore` integration (out of scope for this task, logged for Task 17).
- **Progress:**
  - Fixed 2 owned files (import paths + type collisions); created `frame_draw_screen.dart`; created `test/ui/frame_markup_test.dart` (19/19 green); `flutter analyze` 0 errors, `flutter test test/ui/frame_markup_test.dart` 19 passed.

## [2026-08-22 22:35] ses_fd5559a14ffecM3xZfyzhb0QTJ (final verification)
- **What I learned:** Final verification shows `flutter analyze` clean for owned files and all 19 tests green; no startup-lock issues.
- **Decisions made:** Marked `task.md` Status DONE; no further deviations.
- **Open questions:** none.
- **Progress:** Task 14 complete — files created/verified, LOG appended, task status IN PROGRESS→DONE.
