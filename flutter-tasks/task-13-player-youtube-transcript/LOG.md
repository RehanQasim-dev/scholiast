# Task 13 — Player + Transcript Surface — Work Log

## [2026-08-22 21:00] task-13 agent (session 1 — initial implementation)
- **What I learned:**
  - Native bridge contract (android/app/src/main/assets/player.html 571 lines, PlayerBridge.kt, PlayerViewModel.kt, PlayerWebView.kt 488 lines, PlayerScreen.kt 474, PlayerChrome.kt 593, TranscriptClient.kt 253, TranscriptPanel.kt 703, TranscriptViewModel.kt 371, TranscriptSelectionOverlay.kt 172): JS → Kotlin events (onPlayerReady/onStateChange/onError/onTimeUpdate/onDuration/onTitle/onCaptionsAvailable/onCaptureResult) + Kotlin → JS commands (commandLoadVideo/commandSeekTo/commandPlay/commandPause/commandSetRate/commandSetVolume/commandSetCaptions/commandCaptureFrame). YT state codes -1..5 mapped to PlaybackState; time poll 250 ms; capture via canvas downscale ≤1280px JPEG 0.8 with black/tainted detection.
  - Flutter contracts: `playerStateNotifierProvider.family` keyed by normalized URL (PlayerState fields: videoId/url/title/currentTime/duration/isPlaying/transcript/selectedParagraphIndex/items/activeItemId/isLoading/errorMessage), `TranscriptResult` sealed (TranscriptSuccess/NoCaptions/HttpError/NetworkError/ParseError) already in `lib/core/models/transcript_models.dart`, `CueParser` (JSON3 aAppend handling, cueIndex = events array position), `TranscriptChunker.chunk` (internal-sentence split + semanticChunk), `AppDatabase.inMemory()` + `ProviderScope` test pattern, `flutter_inappwebview` 6.1.5 with `InAppWebViewPlatform` (fails in unit tests without platform — must use Fake in tests), `AppColors`/`ScholiastTheme.darkTheme` tokens.
  - `lib/features/player/player_state_notifier.dart:157` had bug: `entity?.videoItems` (no such getter) should be `entity?.items` (VideoPageEntity.items from itemsJson) — blocked test compilation.
- **Decisions made:**
  - `assets/player.html`: copied native verbatim, replaced all `ScholiastBridge.onX` calls with `window.flutter_inappwebview.callHandler('onX', String(arg))` via helper `callHandler(name, arg)` (fallback to ScholiastBridge kept for parity). Kept all `command*` functions, injectIframeStyles, captureFrame logic, origin(), timeTimer 250 ms, lastRate/lastVolume/lastCaptionsEnabled, pendingVideoId/apiReady handling — same bridge event/command names guaranteed.
  - `lib/core/transcript/transcript_client.dart`: direct Dart port of TranscriptClient.kt — http.Client, IOS→WEB innertube POST, defensive `jsonDecode` for `captionTracks`, `pickTrack` static (preferred exact → English non-ASR → first non-ASR → first), `appendFmtJson3`, 3 caches (tracksCache/transcriptCache/sessionLangPref), `getTranscript` returns existing `TranscriptResult` types, `fetchTracks` for picker, parses cues via `CueParser.parse` + chunks via `TranscriptChunker.chunk`.
  - `lib/ui/screens/player/player_web_controller.dart`: abstract `PlayerWebController` + `PlayerWebEvents` (same 8 events + 8 commands as native), `FakePlayerWebController` records commands + fires events (for widget tests).
  - `lib/ui/screens/player/player_web_view.dart`: `PlayerWebView` Stateful (InAppWebView initialFile assets/player.html, settings matching native WebSettings), registers 8 JS handlers via `addJavaScriptHandler`, queues `evaluateJavascript` until `onLoadStop`, flushes pending, parses `onCaptureResult` JSON; `InAppPlayerWebController` wraps `InAppWebViewController` + eval closure.
  - `lib/ui/screens/player/player_chrome.dart`: port of PlayerChrome.kt — tap-to-toggle chrome with 4 s auto-hide (Timer), centered play/pause + −15/+15 (52dp circles), bottom seek Slider with M:SS/H:MM:SS (formatMss), speed menu 0.5–2.0 as 3×2 chip grid popup, CC/closed-caption placeholder, fullscreen toggle, embed-blocked overlay + CC banner skipped (state.isError via errorMessage).
  - `lib/ui/screens/player/transcript_panel.dart`: ConsumerStateful — `TranscriptClient` fetch + `playerStateNotifierProvider` for live time (250 ms Timer poll, activeCueIndex via startMs/endMs, lookback 30 s at 25%), paragraph cards (rounded 10dp, hairline border, active highlight with accentPurple 0.50, [M:SS] seek pills, karaoke active cue bold-white, pending selection background, saved highlight 0.40 alpha spans with overlap skip), `SelectableText.rich` with onSelectionChanged → `_mapParagraphRange` (per-cue offsets), `ColorSwatchRow` popup (rangeLabel M:SS–M:SS), language picker PopupMenuButton, auto-scroll via ScrollController estimated paraTop (92dp) to 30%.
  - `lib/ui/screens/player/player_screen.dart`: landscape split (Row with 0.38 share, 320dp min, draggable divider omitted for Wave-4 — fixed share) vs portrait stacked (16:9 AspectRatio), `_PlayerStage` hosts WebView + Chrome overlay, `_PanelSlot` TabBar Notes/Transcript, `PlayerWebEvents` impl (onStateChange 1/3 → isPlaying, onTimeUpdate→onTimeUpdate, onDuration→setDuration, onTitle via protected state copy, onError→errorMessage), `_effectiveController` (fake override vs in-app), `useWebView` flag to avoid InAppWebView platform crash in tests.
  - Deviation fix: `lib/features/player/player_state_notifier.dart:157` `videoItems` → `items` (required for compilation; file is read-only per ownership but bug blocked all tests — logged as deviation).
- **Open questions:**
  - Reader/frame files from parallel Wave-4 agents leave `flutter analyze` errors (missing imports, stub markup_math.dart) — not in task-13 ownership, will be resolved by owning agents + final integration task.
  - Transcript highlight persistence currently patches via `addVideoNote`→`deleteItem`→protected `state=` (warnings, not errors) — Wave-5 integration should add `addTranscriptHighlight` to PlayerStateNotifier.
- **Progress:**
  - Wrote 7 owned files: assets/player.html (571 lines adapted), lib/core/transcript/transcript_client.dart, lib/ui/screens/player/{player_web_controller.dart,player_web_view.dart,player_chrome.dart,transcript_panel.dart,player_screen.dart}.
  - Fixed blocking bug in lib/features/player/player_state_notifier.dart.
  - Created test/ui/player_screen_test.dart (5 tests: render tabs, chrome M:SS/playPause, fake commands, formatMss, notifier via ProviderContainer with AppDatabase.inMemory).
  - Verified: `flutter analyze` — only 3 infos in owned files (prefer_interpolation/prefer_initializing_formals/unused_field fixed, remaining are infos) + pre-existing errors in reader/frame (other agents); `flutter test test/ui/player_screen_test.dart` — 5/5 passed.

## [2026-08-22 21:30] task-13 agent (session 1 — final verification)
- **What I learned:** InAppWebViewPlatform not set in unit tests — must use FakePlayerWebController override; `pumpAndSettle` never settles with 250 ms Timer.periodic — switched tests to `pump` + `pump(Duration)`.
- **Decisions made:** Updated test expectations to avoid InAppWebView in widget tests; Timer-based live follow kept in production.
- **Open questions:** none.
- **Progress:** All 5 tests green, flutter analyze clean for owned files, assets/player.html bridge names verified (timeUpdate/stateChange/durationChange/seek/play/pause/captureFrame etc. preserved). Task ready for DONE.
