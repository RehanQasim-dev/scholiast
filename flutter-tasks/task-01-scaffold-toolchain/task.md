# Task 01: Project Scaffold & Toolchain

Status: DONE
Wave: 0
Depends on: —

## Scope & Owned Files
- Create Flutter project `scholiast_flutter` (Android + Linux).
- Configure `scholiast_flutter/pubspec.yaml` with all core dependencies.
- Configure `analysis_options.yaml` with strict linting rules.
- Set up `build_runner` generation configuration.

## Acceptance Criteria
- `scholiast_flutter/` created with `android` and `linux` platform support.
- `flutter pub get` runs cleanly without dependency conflicts.
- `flutter analyze` returns 0 issues on empty project.
