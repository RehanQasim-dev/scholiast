# Task 27 — Pages Sync Spine (LOG)

## [2026-08-24] session 1 — recon
- **What I found:** `assemble_local_page` (task-17) already projects reader tables extension-shape: `HighlightsRepo::get_highlights` rebuilds `HighlightData` (type tag, xpath/startOffset/endOffset, content, color, groupId, updatedAt, `anchor_json` → portable `anchor` passthrough, comments embedded as `notes[]` marker strings); drawings normalize NOT NULL defaults back to None; `diagrams_for_page` emits pointers incl. `imageForHighlight`; article pages get empty `video_id`/`videoItems` and never create `videos` rows (guarded in `apply_page`). Tombstones are composed at push time (`compose_push`: merge(snapshot-as-base-and-remote)) per the task-17 pattern — unchanged.
- **Gaps this task fills:** (1) nothing in production calls `SyncQueueRepo::enqueue` — reader mutations emit `db://changed:*` but the task-18 scheduler only drains hashes already sitting in `sync_queue`, so article annotations never reached Drive. (2) No round-trip tests prove reader-shaped records survive assemble → merge → apply → assemble.
- **Decisions made:** extend `engine.rs` in place rather than adding `reader_apply.rs` (apply path already centralized there; a second module would fragment it). Round-trip tests get a dedicated `sync/reader_apply.rs` test host. Enqueue wiring goes through the sanctioned append-only route in `commands/reader.rs` write commands (scheduler listener approach would require editing task-18-owned control flow).
- **Known limitation logged:** legacy markerless comments (`id` = raw text, created_at=0) mutate on DB round-trip (`format_note(body, 0)` appends `<!--timestamp:0-->`), changing their merge identity. Extension always stamps timestamps on creation, so no real-world data hits this; fixing it would mean editing task-23's `comment_marker_string` (out of my file ownership). Element-highlight `imageEdit` is likewise not column-backed; the association survives via `diagrams.image_for_highlight`.
- **Progress:** status → IN PROGRESS; baseline gates next (127 tests green, clippy clean).

## [2026-08-24 03:05] session 1 — completion
- **What I changed:**
  - `sync/reader_apply.rs` (NEW): 4 round-trip tests — extension-shape assembly of an article page; assemble→merge→apply→assemble fixpoint with a synthetic remote (new highlight h4, newer recolor h3, comment deleted via `h1:<ts>` tombstone); wiremock push-cycle proving a locally deleted highlight rides out ONLY as a tombstone in the pushed JSON and is removed on device B after pull; pages.updated_at touch + videos-table isolation.
  - `sync/engine.rs`: pull-apply now snapshots the RE-ASSEMBLED projection (+ merged tombstones) instead of raw merge output. Raw-merged snapshots never fingerprint-match DB-ordered local state again, permanently breaking the unchanged-skip / queue-drain for any page whose merge order differs from `created_at` order (first reader page with a mid-page insert would have hit it).
  - `commands/reader.rs` (append-only, sanctioned): `enqueue(page)` after every successful write command — save/delete_highlight, update_highlight_color, save/delete_comment, delete_article.
- **Enqueue wiring decision:** option (a) — direct enqueues in reader write commands. Rationale: the task-18 scheduler listens to all reader tables (`LISTENED_TABLES` includes highlights/drawings/diagrams/pages ✓) but its `drain_pages` only pushes hashes ALREADY in `sync_queue`; nothing production-side ever enqueued, so article annotations could never reach Drive. `add_article` deliberately does NOT enqueue: an empty record is skipped by reconcile's `is_empty_record` guard, and source_markdown is not part of PageRecord.
- **Round-trip scenario results (all green):**
  - Article assembly = extension shape: `type:"text"|"element"`, `startOffset/endOffset`, camelCase serde renames verified against models.rs, portable `anchor` JSON passthrough, comments embedded as marker strings (`notes[]`), drawings with None-normalization, diagram pointers via `imageForHighlight`, empty `videoId`/`videoItems`, zero `videos` rows.
  - Merge+apply fixpoint on fresh DB B: h1 thread reduced to exactly `first comment<!--timestamp:1700000000000-->` (id `1700000000000` preserved byte-exact in `comments.id`), h3 newest-wins black@12000, h4 added, tombstone `h1:1700000005000` retained, second apply cycle fingerprint-stable.
  - Delete→push→pull chain: pushed JSON `highlights == [h1,h3]` AND `tombstones.highlights["h2"]` present; B downloads, drops h2 from rows, keeps untouched threads, adopts rev "2".
- **Deviations / notes for orchestrator:**
  1. Equality across apply is asserted modulo array order (`canonical()` sorts by id): SQLite orders by `created_at,id`, the TS-faithful merge by first-seen insertion; neither is sync truth and both are stable per store. Golden byte-ordering of merge output remains pinned by crates/core fixture tests (untouched).
  2. Concurrent task-25 agent worked in this tree during my session: their `extract.rs`/`error.rs`/`reader.rs` edits transiently broke compile/clippy mid-flight; final workspace gates pass with their landed state. Their reader.rs rewrite preserved my enqueue calls but dropped my trait import once — re-added.
  3. Known pre-existing limitation (documented, not fixed): legacy markerless comments mutate identity on DB round-trip (`<!--timestamp:0-->` appended by `comment_marker_string`); extension data always carries timestamps so unreachable in practice. Element-highlight `imageEdit` is not column-backed — association survives via `diagrams.image_for_highlight`; rehydration belongs to the image-edit feature wave if desktop ever edits images.
  4. Expected-record assertions are inline rule-based rather than a new JSON fixture file: merge OUTPUT byte-behavior already has golden fixtures (task-02/17); this task's new surface is the store↔record mapping, which fixtures can't express without duplicating them.
- **Cross-client note (for task-32):** an article annotated here now round-trips through the extension's Drive layout — same `pages/page-<sha16>.json` name (`normalize::page_file_name`), PageRecord v2 with extension field names (serde-pinned by golden_models), comments as inline-marker strings inside each highlight, drawings/diagram pointers beside video fields. Pulling that file into the extension dashboard requires no translation beyond what `shared/merge.ts` already does; manual verification scripted in task-32 should annotate one article here (highlight + 2 comments + recolor), sync, open the extension dashboard on another device profile, and confirm quote/comment/color fidelity plus tombstone non-resurrection.
- **GATES:** `cargo clippy --workspace --all-targets -- -D warnings` CLEAN · `cargo test --workspace` 160 passed / 0 failed (baseline 127; +4 mine, rest concurrent task-25) — no regression.
- **Progress:** ALL DELIVERABLES DONE → status DONE.
